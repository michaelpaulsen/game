/*define([], function () {
	var Npc = function () {
		this.npc ={
			posX : math.random * 20,
			posY : math.random * 20,
			id : 10,
			type : 1,
			hp:100
      eggzest = true;
	  }
/* general NPC movement might make monsters come to you*/
		/*while(npc.eggzest===true){
			var a = Math.floor( this.generator.random() * 4 );
			if (a === 0.25){ 
				this.npc.posX--;
			}
			if (a === 0.50){
				this.npc.posY--;
			}
			if (a === 0.75){
				this.npc.posX++;
			}
			if (a === 1.00){
				this.npc.posY++;
			}
		}

		return Npc;
/*maybe we would have some someting like if(this.npc.type===1){
 var a = Math.floor( this.generator.random() * 4 );
	if(a>== 0.25){ 
			this.npc.posX--;
	}
	if(a>== 0.50){
		this.npc.posY--;
	}
	if(a>== 0.75){
		this.npc.posX++;
	}
	if(a>== 1.00){
		this.npc.posY++;
	}
	}
});
*/